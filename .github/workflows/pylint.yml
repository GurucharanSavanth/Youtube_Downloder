name: YouTube Video Downloader

on:
  push:
    branches:
      - main

jobs:
  download-video:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: pip install pytube
      - name: Download video
        run: python main.py ${{ env.video_url }}

  download-playlist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: pip install pytube
      - name: Download playlist
        run: python main.py ${{ env.playlist_url }} 
